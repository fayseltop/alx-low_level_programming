#include "main.h"
#include <string.h>

/**
*  _strlen -> function to get the length of string.
* @s: string pointre to passed to this function.
*Return: rreturn length of the string.
*/
int _strlen(char *s)
{
return (strlen(s));
}
